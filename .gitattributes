# LUT assets are opaque generated files — don't line-end normalize or diff.
assets/luts/*.cube binary

# Doc screenshots / animations — treat as opaque binary.
docs/*.png binary
docs/*.gif binary
docs/*.jpg binary
